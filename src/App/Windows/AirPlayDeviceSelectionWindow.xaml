<ui:FluentWindow x:Class="IPhoneMirror.App.Windows.AirPlayDeviceSelectionWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:animations="clr-namespace:IPhoneMirror.UI.Animations" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
 Title="选择 AirPlay 设备" Width="460" Height="410" MinWidth="400" MinHeight="340" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" WindowCornerPreference="Round" Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
 <Grid Style="{StaticResource SubWindowPageRoot}" animations:PageTransition.IsEnabled="True"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="16"/><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Grid Style="{StaticResource SubWindowHeader}" animations:WindowDragBehavior.IsEnabled="True"><TextBlock Text="选择 AirPlay 设备" Style="{StaticResource SubWindowTitle}"/><Button Style="{StaticResource SubWindowCloseButton}" Click="CancelClick" ToolTip="取消"><ui:SymbolIcon Style="{StaticResource ButtonSymbolIcon}" Symbol="Dismiss20" FontSize="18"/></Button></Grid>
  <StackPanel Grid.Row="2"><TextBlock Text="添加 AirPlay 设备档案" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="选择当前正在镜像的 AirPlay 设备以创建档案。" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel>
  <ListBox Grid.Row="4" ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice, Mode=TwoWay}" BorderThickness="0" Background="Transparent"><ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,0,8"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Root" Background="{DynamicResource ControlFillBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource ControlHoverBrush}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource ComboItemSelectedBrush}"/><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style></ListBox.ItemContainerStyle><ListBox.ItemTemplate><DataTemplate><StackPanel><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/><TextBlock Text="{Binding ModelDisplay}" FontSize="11" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,0"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox>
  <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="取消" Click="CancelClick" MinWidth="88"/><Button Content="创建档案" Click="ConfirmClick" IsEnabled="{Binding CanConfirm}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" MinWidth="108"/></StackPanel>
 </Grid>
</ui:FluentWindow>
